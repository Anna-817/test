import React, { useEffect, useState, useCallback, useMemo, useRef } from "react";

import './header.scss';

export const Header = () => {
    return (
        <h1 className="header">
            Hello React
        </h1>
    );
  };

  export default Header;